{
  "name": "@posbar/print-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Agente de impresión en sitio. Esqueleto: se implementa en la fase 18.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "test": "vitest run --passWithNoTests"
  }
}
