# Servicio Coolify: pos-web  (Dockerfile: deploy/web/Dockerfile)
# Es un ARG de compilación, no una variable de ejecución: marcarlo como "Build Variable" en Coolify.
VITE_API_URL=https://api.ejemplo.com
